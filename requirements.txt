# Kaggle-friendly runtime requirements.
# Keep these as ranges so managed notebook environments can use their
# preinstalled binary wheels instead of forcing old lock-file builds.
Pillow>=9.3.0
scikit-image>=0.19.3
torch>=1.13.1
torchvision>=0.14.1
gradio>=4.44.1,<7.0
